[project]
name = "nexjob-scrapper"
version = "0.1.0"
description = "Multi-source job scraper for Indonesian job portals (Loker.id, JobStreet, Glints)"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.14.2",
    "gspread>=6.2.1",
    "oauth2client>=4.1.3",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "supabase>=2.0.0",
]
